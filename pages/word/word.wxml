<!--index.wxml-->
<view class="container">
  <view class="conten">
    <view class="word-item" wx:for="{{world}}">
      <image class="word-img" src="{{item.text}}" data-to="{{item.disable}}" bindtap="toWord" />
    </view>
  </view>
</view>
